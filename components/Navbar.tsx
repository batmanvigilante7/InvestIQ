"use client";

// Navbar is no longer used — Sidebar handles branding and navigation.
// This file is kept empty to avoid import errors from old references.
export default function Navbar() {
  return null;
}
